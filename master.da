from validator import Validator
import uuid

def main():
    config = {
        'id':uuid.uuid4()
    }
    validator_set = new(Validator,num=nprocs)
    for i in range(0,len(validator_set)): setup(validator_set[i],(config,validator_set,i))
    start(validator_set)
